# Environments
.venv/
env/
venv/
ENV/

# SQ3 experiment artifacts (runs.jsonl + pairs.jsonl + summary.md + sources/)
# Regenerated by `python -m sq3.cli`; not for VCS.
/results/

# IDEs and Text Editors
.idea/
.vscode/
*.swp
*.swo

# Python caches and compiled files
__pycache__/
*.py[cod]
*$py.class

# Pytest
.pytest_cache/

# Build artefacts
*.egg-info/
dist/
build/

# Tooling caches
.mypy_cache/
.ruff_cache/

# Coverage
.coverage
htmlcov/

# Node / pnpm (when skills/ gets compiled by teammate D)
node_modules/
pnpm-lock.yaml

# uv
.uv/

# Local trustroom socket
*.sock

logs/
# Runtime logs (signed audit logs, agent action logs, harness output)
*.log

# Local per-agent DelftClaw/OpenClaw configs
configs/*.local.env
# Per-host LLM config — holds the real upstream API key.
# Tracked example lives at configs/.env.example.
configs/.env

# pull_sync_demo writes signed logs + per-source peer caches here for
# inspection; regenerated on every run
examples/pull_sync_artifacts/
